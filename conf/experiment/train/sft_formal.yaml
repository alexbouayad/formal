# @package _global_

defaults:
  - default
  - _self_

trainer:
  rws: null

input:
  config: "${get_language_id:${formalization.language}}"

output:
  config: "${get_language_id:${formalization.language}}-sft_formal"
