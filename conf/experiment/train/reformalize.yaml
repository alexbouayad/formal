# @package _global_

defaults:
  - default
  - _self_

input:
  config_name: "${get_language_id:${formalization.language}}-${hydra:runtime.choices.formalization}"

output:
  config_name: "${get_language_id:${formalization.language}}-${hydra:runtime.choices.formalization}"
