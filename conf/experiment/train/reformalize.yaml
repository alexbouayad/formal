# @package _global_

defaults:
  - default
  - _self_

input:
  config: "${get_language_id:${formalization.language}}-${hydra:runtime.choices.formalization}"

output:
  config: "${get_language_id:${formalization.language}}-${hydra:runtime.choices.formalization}"
