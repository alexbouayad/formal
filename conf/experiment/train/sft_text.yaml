# @package _global_

defaults:
  - default
  - override /formalization: text_only
  - _self_

trainer:
  rws: null

input:
  config: "${get_language_id:${formalization.language}}-textual"

output:
  config: "${get_language_id:${formalization.language}}-sft_text"
