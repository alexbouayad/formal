defaults:
  - base_ingest
  - wandb: default
  - runtime: default
  - experiment/ingest: default
  - _self_

hydra:
  job:
    name: ingestion
  sweeper:
    params:
      language: "${sweep_all_languages:}"
