defaults:
  - base_tokenize
  - wandb: default
  - runtime: default
  - experiment/tokenize: default
  - _self_

hydra:
  job:
    name: tokenization
  sweeper:
    params:
      formalization.language: ${sweep_all_languages:}
